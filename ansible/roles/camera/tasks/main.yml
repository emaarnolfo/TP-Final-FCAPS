---
- name: Instalar Apache para simular cámara
  apt:
    name: apache2
    state: present

- name: Crear página simulada para la cámara
  copy:
    dest: /var/www/html/index.html
    content: |
      <html>
      <head><title>{{ camera_name }}</title></head>
      <body><h1>Cámara {{ camera_name }}</h1></body>
      </html>

- name: Habilitar y arrancar Apache
  service:
    name: apache2
    state: started
    enabled: yes

